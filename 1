import ccxt

# Initialize Bybit exchange
exchange = ccxt.bybit()

# Fetch all markets
markets = exchange.load_markets()

# Filter pairs against USDT
usdt_pairs = [symbol for symbol in markets if symbol.endswith('/USDT')]

# Display the pairs
print("Bybit USDT pairs:")
for pair in usdt_pairs:
    print(pair)